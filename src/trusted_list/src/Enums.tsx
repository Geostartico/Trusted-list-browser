import React, { Component } from 'react';

export enum FilterType {
    Country, // id: 0
    Type, // id: 0
    State, // id: 0
    Provider, // id: 0
};

export enum SelectionType {
    Selectable,
    Selected,
    NotSelectable,
};
